import SwiftUI

// Intentionally empty — use system colors, fonts, and spacing directly.
// .primary, .secondary, .font(.body), .font(.headline), etc.
// Only add values here if they have no direct system equivalent.
enum AppTheme {}
